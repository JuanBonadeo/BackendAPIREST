const mascotas = []

export const guardarMascota = (mascota)=>{
    mascotas.push(mascota)
}

export const leerMascot = (mascota)=>{
    console.log(macota)
}

// todas las funciones para modificar los datos, traer los datos de la DB